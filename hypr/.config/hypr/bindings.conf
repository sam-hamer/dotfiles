# Application bindings
$terminal = uwsm app -- ghostty
$browser = omarchy-launch-browser

$hyper = MOD3

bindd = $hyper, return, Terminal, exec, $terminal --working-directory=$(omarchy-cmd-terminal-cwd)
bindd = $hyper, F, File manager, exec, uwsm app -- nautilus --new-window
bindd = $hyper, B, Browser, exec, $browser
bindd = $hyper, T, Activity, exec, $terminal -e btop
bindd = $hyper, N, Obsidian, exec, uwsm app -- obsidian -disable-gpu
bindd = $hyper, slash, Passwords, exec, uwsm app -- 1password
bindd = $hyper, D, Discord, exec, uwsm app -- discord

# If your web app url contains #, type it as ## to prevent hyperland treat it as comments
bindd = $hyper, A, ChatGPT, exec, omarchy-launch-webapp "https://chatgpt.com"

# Overwrite existing bindings, like putting Omarchy Menu on Super + Space
# unbind = SUPER, SPACE
# bindd = SUPER, SPACE, Omarchy menu, exec, omarchy-menu

# Application bindings
# $terminal = uwsm app -- ghostty
# $browser = uwsm app -- zen-browser
# $discord = uwsm app -- discord
# $webapp = $browser --app

$hyper = MOD3

# bind = $hyper, return, exec, $terminal
# bind = $hyper, F, exec, uwsm app -- nautilus --new-window
# bind = $hyper, B, exec, $browser
# bind = $hyper, N, exec, uwsm app -- obsidian -disable-gpu
# bind = $hyper, T, exec, $terminal -e btop
# bind = $hyper, D, exec, $discord
# bind = $hyper, slash, exec, uwsm app -- 1password
